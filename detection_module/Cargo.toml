[package]
name = "detection_module"
version = "0.1.0"
edition = "2024"

[lib]
name = "detection_module"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
